export class Project {
	constructor(id, name, description, projectStack, sourceCodeLink, webSiteLink) {
		this.id = id;
		this.name = name;
		this.description = description;
		this.projectStack = projectStack;
		this.sourceCodeLink = sourceCodeLink;
		this.webSiteLink = webSiteLink;
	}
}